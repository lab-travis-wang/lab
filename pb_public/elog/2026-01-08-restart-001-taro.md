# 2026-01-08 重启实验 001：迁移至 Taro 框架

- **created_at**: 2026-01-08T16:00:00+08:00
- **related_tasks**: ["restart-001"]

## 背景
根据 `001/PLAN.md` 的要求，实验 001 需要支持发布为微信小程序。之前的项目结构可能不够灵活或未针对小程序环境进行配置。

## 方案选择
决定采用 **Taro (React + TypeScript)** 框架。Taro 是一个多端开发解决方案，能够编译成微信小程序、H5、React Native 等多端应用，非常符合目前的实验需求。

## 执行记录
1. **项目初始化**：在 `001/` 目录下使用 Taro CLI 初始化了项目。
2. **元数据同步**：创建了符合 `lab/SPEC.md` 规范的 `001/experiment.json`。
3. **依赖安装**：通过 `bun i` 安装了 Taro 核心依赖，并额外安装了 `pocketbase` SDK 用于数据存储。
4. **任务更新**：在 `lab/pb_public/elog/tasks.md` 中记录了重启任务。

## 下一步计划
- 设计 MVP 核心 UI（素材记录面板、AI 对话界面）。
- 集成移动端 / 小程序端的 AI 追问逻辑。
- 对齐 CSS 方案，考虑是否引入 `coss-ui` 的组件风格。
